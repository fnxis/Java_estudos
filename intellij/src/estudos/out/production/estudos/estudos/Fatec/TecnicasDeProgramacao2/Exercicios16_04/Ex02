A: Before: 0 After: 1Flap Flap: 0 , o metodo getRefCount busca a variavel statica referenceCount que inicialmente foi dada por 0, depois de executar o new Nightingale() o construtor é chamado e o comando referenceCount++ é executado, incrementando em 1 a variavel, e por fim o metodo fly() imprime o valor de a, e como nao foi inicializado, por padrao o valor é 0
B: Da variavel ReferenceCount somente 1 instancia, pois é uma variavel statica, entao somente sera criada em uma instancia, ja a variavel (a) sera criada 15 vezes pois ela é uma variavel de instancia, e como nightingale herda bird, ela possui a variavel (a) tambem, por isso serao 15.